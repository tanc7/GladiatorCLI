# in the event that your system Python is borked, you should run the virtualenv installer (this) instead. Which safely creates a virtual environment of Python to ALLOW for non-system breaking changes to Python to be run.
# It also has the advantage of permitting stable usage of the bleeding edge repositories for Python 3.x, Django, and Tornado
